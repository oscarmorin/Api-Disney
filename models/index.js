const Character = require('./character');
const Genre = require('./genre');
const MovieSerie = require('./movieSeries');
const Role = require('./role');
const Server = require('./server');
const User = require('./user');

module.exports = {
    Character,
    Genre,
    MovieSerie,
    Role,
    Server,
    User
}